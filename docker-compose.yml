version: '2.2'
services:
  server: 
    image: node:alpine
    working_dir: /app
    restart: unless-stopped
    ports:
      - "7000:7000"
    command: "./wait-for-mongo.sh"
    depends_on:
      - "mongo"
    links:
      - "mongo:mongo"
    volumes:
      - './server:/app'

  mongo:
    restart: unless-stopped
    image: mongo:latest
    volumes:
      - './mongo:/data/db'